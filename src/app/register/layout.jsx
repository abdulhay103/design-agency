export async function generateMetadata() {
    return {
        title: "Design-Agency || Register",
    };
}

export default function RegisterLayout({ children }) {
    return <div>{children}</div>;
}
